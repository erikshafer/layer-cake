# Build log

Running record of decisions made during build sessions: anything a later session (or the deck) would otherwise re-derive. One dated line or short block per decision. Newest at the bottom. This intentionally replaces the heavier prompt/retrospective pipeline used in the author's other repos; the talk is the deadline.

- **2026-08-23** Scaffold delivered (37 files). After twin named `LayerCake.Slices`. .NET 10 / C# 14. MediatR 12.5.0 + AutoMapper 14.0.0 pinned at final OSS releases (deliberate; Act 2 line). Versions frozen per `Directory.Packages.props`.
- **2026-08-23** First `dotnet build` fixes: (1) `Microsoft.EntityFrameworkCore` pin bumped 10.0.3 → 10.0.4 — the Npgsql provider's patch numbers do not track EF Core's, and Npgsql 10.0.3 requires EF Core >= 10.0.4 (NU1605 restore failure otherwise). Pre-freeze correction, not a freeze violation; the 10.0.3 pin never restored. (2) AutoMapper 14.0.0's high-severity advisory (GHSA-rvv3-g6hj-g44x) suppressed via a commented `NuGetAuditSuppress` in `Directory.Build.props` — the fixed 15.x releases are commercial, and the frozen-at-final-OSS pin is the talk's point. Attendees get a clean clone-and-build; the comment tells the story. Solution now builds with 0 warnings, 0 errors.
- **2026-08-23** After twin needs `WolverineFx.RuntimeCompilation` (pinned 6.28.1, matching the Wolverine line): Wolverine 6 decoupled the Roslyn runtime compiler from core, so `TypeLoadMode.Dynamic` (the dev default we run everywhere, tests included) throws at startup without it. Referenced in `LayerCake.Slices.csproj`; auto-registers. Static/pre-generated codegen deliberately not adopted — one less moving part before the talk, and attendees who clone get the just-works path. Considered and rejected the mmo-reconnect hybrid (Dynamic in dev, Static + committed generated code in prod, Debug-gated package): its payoffs are production ones (image size, runtime-codegen memory) this repo doesn't have, and it imports a forget-codegen-write 404 failure mode plus committed codegen churn that would muddy the file-count slide. The production story is instead documented as a comment on the `WolverineFx.RuntimeCompilation` reference in `LayerCake.Slices.csproj`, pre-answering the "what about prod?" audience question.
- **2026-08-23** Contract decisions settled (previously DRAFT in the presentations repo's api-contract.md): two schemas in ONE database (`before`/`after` in `layercake`); PublishCake keeps the duplicate-name 409 guard; coupon validation is an always-200 discriminated envelope; error parity = exact status + content type + reason-discoverable-in-body via one shared assertion helper; `GET /cakes/{id}` stays (Location target). Agent-facing slice specs authored in `docs/slices/`.
- **2026-08-23** Slice 001 built end to end (change `slice-001-publish-browse-cakes`); shared suite green on both hosts, 18/18. Decisions made along the way:
  - **EF Core migrations, not `EnsureCreated`** (the leaning, confirmed): `InitialCreate` lives in `Infrastructure/Persistence/Migrations`, applied at startup in Development so `docker compose up -d` + run/test stays the whole ritual. `Microsoft.EntityFrameworkCore.Design` 10.0.4 added (design-time only, matches the EF pin; addition not a version bump, freeze intact).
  - **Marten camelCase set explicitly upfront**: `opts.UseSystemTextJsonForSerialization(casing: Casing.CamelCase)` (`Casing` lives in `Weasel.Core` in Marten 9). Stored JSON verified camelCase in `after.mt_doc_cake`.
  - **After-twin 201 response implements `IHttpAware` directly** instead of subclassing `CreationResponse`: the subclass serializes the base `Url` property into the body as an extra `url` field, breaking the contract body shape. `CakePublished` writes its own 201 + Location.
  - **After-twin 404 uses `[Entity(Required = true, OnMissing = OnMissing.ProblemDetailsWith404)]`** so the miss is `application/problem+json` with the reason in the body, matching the error-parity rule (and the before twin's exception-filter 404).
  - **One `CakeDto` for all three endpoints**: browse/get item bodies include `publishedAt` (a superset of the slice spec's minimal item shape). Keeps the twins byte-consistent with each other and avoids a second DTO; the after twin returns the `Cake` document as-is.
  - **Scenario 1 publishes "Opera"**, not the spec's example "Chocolate Stout": Chocolate Stout is a seed cake, so it is the natural payload for the duplicate-409 scenario instead.
  - **Per-twin xUnit collections** serialize scenario classes for the same twin (parallel hosts raced on startup migration/seeding); the two twins still run in parallel with each other. Fixtures reset (EF `ExecuteDelete` / `CleanAllMartenDataAsync`) and re-seed per scenario class.
  - **Location header**: before twin's `CreatedAtAction` emits an absolute URL, after twin a relative one; the suite asserts it ends with `/cakes/{id}`. Recorded as accepted divergence (header value, not body).
- **2026-08-23** Slice 002 built end to end (change `slice-002-validate-coupon`); shared suite green on both hosts, 30/30 (18 from slice 001 + 6 coupon scenarios × 2 twins). Decisions made along the way:
  - **Per-twin clock sourcing** (per the change's design.md): the shared evaluation is `(Coupon?, DateTimeOffset now) -> CouponStatus` on both sides. After twin passes `DateTimeOffset.UtcNow` inline at the endpoint; before twin injects `IDateTimeProvider` (Application interface, Infrastructure implementation) into `CouponValidationService`. The ceremony asymmetry is the exhibit; tests need no time control because seed windows sit ±1 year/±1 day from now.
  - **Wire-status mechanics differ per twin, wire-identical result**: before twin's query handler maps the Domain `CouponStatus` enum to the contract string with an explicit switch (no JSON attributes in Domain); after twin's `CouponStatus` enum serializes itself via `JsonStringEnumConverter<T>` + `[JsonStringEnumMemberName]` (.NET 9+). Both emit exactly `invalid | notYetActive | expired | valid`.
  - **`percentOff` absent, not null**, via `[JsonIgnore(Condition = WhenWritingNull)]` scoped to the envelope DTO/record on both sides; the suite asserts the raw body never contains `percentOff` for non-valid statuses (and pins camelCase keys in the raw body, since `JsonSerializerOptions.Web` deserialization is case-insensitive).
  - **Coupon code is the after twin's Marten document identity** (`[Identity]` on `Code`, stored uppercase); lookup is one `ToUpperInvariant()` then `LoadAsync`. Before twin: Guid PK + unique index on uppercase `Code`. Deliberately NOT `[Entity]` on the after endpoint: the route value needs normalizing first, and a miss is the normal `invalid` answer, never a 404.
  - **`[Identity]` moved to the `JasperFx` namespace** in the JasperFx 2.0 line (Marten 9); it is no longer `Marten.Schema.IdentityAttribute`. Comment left at the use site.
  - **After twin `SeedData` moved** from `Cakes/SeedData.cs` to the project root (`LayerCake.Slices.SeedData`) since it now seeds two modules (cakes + coupons). Coupon seeding is a plain `Store` upsert (code = identity), so idempotency is by construction; verified both twins hold exactly three coupons after seeding twice.
- **2026-08-24** Git initialized (`main`), initial commit `eb8cc3a` (scaffold + OpenSpec trail + slices 001-002, 122 files). Private GitHub remote created: https://github.com/erikshafer/layer-cake. Visibility flips to public closer to the talk, not before.
- **2026-08-24** GitHub Actions CI added (`.github/workflows/ci.yml`): postgres:17 service mirroring docker-compose, .NET 10 Release build, shared contract suite on push/PR. Green from the first run (30/30 on both hosts).
- **2026-08-25** Slice 003 built end to end (change `slice-003-place-order`); shared suite green on both hosts, 52/52 (30 from slices 001-002 + 11 order scenarios × 2 twins). Decisions made along the way (design.md calls confirmed in practice):
  - **Repository-only baker ceremony** (per the change's design.md): one `IBakerTaskRepository` covers the write and the to-do read; no separate `IBakerNotifier` (would be padding). The "imagine this line is SendGrid" comment sits on the handler's inline `Add` call.
  - **`IUnitOfWork` introduced for the single-save transaction**: `LayerCakeDbContext` implements it, `IOrderRepository.Add`/`IBakerTaskRepository.Add` stage only, and `PlaceOrderCommandHandler` commits once, so order + baker task are literally one transaction. Deliberate asymmetry with the self-saving cake/coupon repositories from slices 001-002, documented on the interfaces: multi-aggregate atomicity is exactly the case the classic unit-of-work ceremony exists for.
  - **`CouponStatusWire` extracted** (Application/Coupons): slice 002's enum-to-wire-string switch now has two consumers (ValidateCoupon envelope, InvalidCouponException detail), so it moved from a private method to the one shared mapper.
  - **After-twin load-once compound worked, no fallback needed**: `LoadAsync` returns `PlaceOrderData(cakes, coupon)`, Wolverine passes it into both `Validate` and `Post`, and the pure `Decide(lines, cakes, coupon, placedAt)` function needs no session. Guard order preserved inside `Validate` (400 lines/quantity, 422 unknown ids listed, 422 coupon status via `CouponValidation.Evaluate`); the failing coupon status is camelCased into the detail with `JsonNamingPolicy.CamelCase.ConvertName`.
  - **After-twin exactly-once by identity upsert**: `BakerTask.Id` = order id, so `NotifyBakerHandler`'s `Store` is idempotent under the outbox's at-least-once redelivery. Before twin has no duplication path (inline same-transaction write) and keeps a conventional own-id entity.
  - **`OrderPlaced` follows the slice 001 `IHttpAware` pattern** (201 + Location without an extra body field); `couponCode` absent-not-null via the same `[JsonIgnore(WhenWritingNull)]` idiom as `percentOff`, asserted on the raw body.
  - **Totals stored at placement, reads return the snapshot**: both twins persist lines (with name/unitPrice/lineTotal), subtotal, discount, total; `GET /orders/{id}` recomputes nothing.
  - **`PlacedAt` read-back tolerance 1 ms** in `get_order_by_id_returns_200_with_same_shape`: PostgreSQL `timestamptz` stores microseconds, so the before twin's EF read-back drops the sub-microsecond ticks its POST response carried (Marten's stored JSON keeps full precision). Accepted storage-precision divergence, commented in the scenario.
  - **Poll constants**: 250 ms interval, 5 s timeout, last response body in the failure message; the baker scenario polls until present then asserts exactly one.
  - **Fixture reset for Orders uses raw SQL** (`DELETE FROM "before"."Orders"`): EF `ExecuteDelete` and owned collections don't mix, and the FK's ON DELETE CASCADE clears the lines table.
- **2026-08-25** Pre-freeze JasperFx upgrade pass (session prompt `docs/upgrade-jasperfx-packages.md`, deleted with this PR as it instructed):
  - **Wolverine line 6.28.1 → 6.30.0** (WolverineFx.Http / .Marten / .RuntimeCompilation in lockstep) and **Marten 9.24.0 → 9.29.0** (+ Marten.AspNetCore). Freeze header re-pinned 2026-08-25; the Marten floor is now 9.29.0 because it fixes silent data loss on the commit path (a retried commit could send empty parameter buffers, and commits are no longer replayed when the outcome is unknown). Wolverine 6.29.2 adds a partial index on `owner_id` in the message store, applied automatically in dev. Release-note sweep found nothing breaking for a state-stored document app; 0 build warnings, suite 52/52 twice, no codegen or serialization shifts. Before-twin pins untouched.
  - **CritterWatch: ADD, Erik's explicit call** at the decision checkpoint (the alternative was skip-and-log). Pinned CritterWatch 1.0.1 + Wolverine.CritterWatch 1.0.1 + WolverineFx.RabbitMQ 6.30.0. New console host `src/monitor/LayerCake.CritterWatch` (port 42030) with its own `critterwatch` Postgres database (dedicated DB, not a schema; `docker/postgres-init/` creates it on fresh volumes, existing volumes need the one-off `CREATE DATABASE critterwatch`). RabbitMQ added to docker-compose as the telemetry channel. The after twin publishes telemetry ONLY when `CritterWatch:Enabled` is true (launchSettings env var), so `dotnet test` never needs RabbitMQ or the console. License key goes in user secrets (`JasperFx:LicenseKey`, id `layercake-critterwatch`) — NOT set yet; in Development the console runs license-free with all features enabled.
  - **The 0.9.x install playbook needed two 1.0 corrections** (per the official quickstart): the intake queue is `.ProcessInParallelWithNativeAcks().UseCritterWatchSerializer()` — Wolverine 6.30's new listener-mode validation refuses the old Inline/Sequential shape at startup — and clustering is opt-in in 1.0, so `enableClusterPartitioning: false` is no longer needed. Gotcha for reruns: changing the intake queue's DLQ arguments after a failed boot requires `rabbitmqctl delete_queue critterwatch`; RabbitMQ rejects inequivalent redeclaration.
  - **End-to-end verified live**: console 200 on 42030, `critterwatch` queue 1 consumer / 0 backlog, `layercake-control` 1 consumer, service "LayerCake" ingested into the console's store. CI unaffected (tests are broker-free; the console just builds).
- **2026-08-25** Frontend built (session spec `docs/frontend.md`): `src/frontend/index.html` (300 lines, one file, inline CSS/JS, opens from `file://`), symmetric Development-only CORS in both twins (+5 lines each), README section, CLAUDE.md updates. Decisions made along the way:
  - **CORS placement**: `builder.Services.AddCors()` in the services block; `app.UseCors(...)` as the FIRST line inside the existing `IsDevelopment()` branch on both twins, before `UseSwagger()`, well before endpoint mapping. Inline policy (`AllowAnyOrigin` + any header/method + `WithExposedHeaders("Location")`), no named policy, identical comment both sides pointing at `src/frontend/`.
  - **Quantity inputs default to a blank field with a `0` placeholder**, resolving a spec tension: the spec formula ("every row with quantity > 0") would make the acceptance case "one quantity 0 and one quantity 1: 400" unreachable and would itself be pre-validation, which the spec forbids. Resolution: blank = not part of the order; anything typed (an explicit 0 included) is sent exactly as entered. All acceptance cases pass under this reading and both order 400s stay one keystroke away in a demo.
  - **Typed quantities survive the switch keyed by cake name** (ids differ per twin, so rows rebuild from the active twin's catalog response; the typed text carries, per the "form inputs carry" rule).
  - **Reason phrases via a small fallback table** (`res.statusText` first): Kestrel over HTTP/1.1 supplies them today, but `statusText` is legitimately empty over HTTP/2, so the pane never shows a bare status number.
  - **`replaceAll` avoided** (ES2021); the page keeps to the spec's ES2020 with regex `replace`.
  - **Acceptance walk observation**: the before twin's FluentValidation 400s (blank name, order line guards) carry no `detail`, so the page shows the `title` ("One or more validation errors occurred.") per the spec's detail-or-title rule; the after twin's guards put the reason in `detail` ("Name is required"). Same status, same content type, reason discoverable in the body on both sides: the established error-shape parity, visible on the page as a body-wording divergence.
- **2026-09-02** Contract suite moved to **Testcontainers** (Erik's call: low effort, low risk, high payoff for demos and dev; pattern lifted from mmo-reconnect). `Testcontainers.PostgreSql` 4.14.0 added to the freeze file as an addition, not a bump. Decisions made along the way:
  - **One `postgres:17` container per twin collection** (`ICollectionFixture<PostgresContainerFixture>`), not one shared container: xUnit 2.9 class fixtures can take collection fixtures, so each host fixture receives its twin's container and overrides `ConnectionStrings:Postgres` through Alba's `UseSetting`. No production code changed. The two collections still run in parallel, now on separate databases; "one database, two schemas" stays a runtime exhibit, not a test-topology one.
  - **Fixtures own schema creation**: the before twin's `MigrateAsync` only ran inside Program.cs's `IsDevelopment()` branch, so the old suite silently relied on a previously migrated compose volume. The fixture now calls `MigrateAsync` itself; the after twin's counterpart is `ApplyAllConfiguredChangesToDatabaseAsync` before the wipe-and-reseed. Same image as docker-compose so the suite proves against the demo engine.
  - **CI drops its Postgres service** (`ubuntu-latest` ships Docker, Testcontainers starts what it needs). docker-compose stays for the live demo only; README and CLAUDE.md reordered so `dotnet build` + `dotnet test` is the whole ritual and compose belongs to "run a twin live". Verified 52/52 with the compose Postgres stopped.
- **2026-08-25** Post-frontend staleness cleanup (session prompt `docs/post-frontend-cleanup.md`, deleted with this PR as it instructed): README out-of-scope sentence reworded so it no longer contradicts "The frontend" section (boundary is now: no frontend framework or SPA, demo page off the critical path and not part of the proof); `docs/frontend.md` status flipped GO to BUILT (PR #3, merge `cdb580a`, suite 52/52), DECK-fence sentence untouched; frontend page and docs surfaced as solution items in `LayerCake.slnx` (zero build impact). Out-of-repo follow-ups for Erik, recorded here so they are not lost with the prompt: presentations repo plan.md decision 13 (build fence lifted, page BUILT 2026-08-25 via PR #3, DECK fence still stands) and the scorecard slide line counts (1,701 / 692, re-source from `docs/file-inventory.md`); mirror both into the "Presentations & Talks" Claude project notes.
- **2026-09-04** State-of-the-repo audit, Tier 1 fixes. The before twin's seeder skipped coupons that already existed, so their relative windows (HOLIDAY30 = starts tomorrow) froze at first seed. On a compose volume seeded more than a day earlier, the before twin reported HOLIDAY30 `valid` while the after twin (identity upsert, refreshed every startup) reported `notYetActive`; `POST /orders` with it gave 201 on one twin and 422 on the other. The Testcontainers suite never sees this (fresh database every run), so it was a live-demo-only parity break. Fix: `LayerCakeDbContextSeeder` now refreshes `PercentOff`/`StartsAt`/`ExpiresAt` on existing coupons, matching the after twin's behaviour. Verified by tampering `HOLIDAY30.StartsAt` to ten days ago on the compose database and restarting the before twin: window restored, endpoint back to `notYetActive`, still three coupons. Suite 52/52 on Testcontainers. Remaining audit items (orphaned `Marten.AspNetCore` pin, stale compose line in `openspec/config.yaml`, after-twin duplicate-name race, before-twin missing exception fallback, assorted stale comments) are queued, not done.
