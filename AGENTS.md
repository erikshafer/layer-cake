# Agent Instructions

This repository's AI guidance lives in [`CLAUDE.md`](CLAUDE.md). It is tool-agnostic: whatever agent or assistant you are, read `CLAUDE.md` first and follow it.

Highlights it owns:

- What LayerCake is (a before/after twin laboratory for a conference talk) and the non-negotiables.
- The shared contract-test discipline in `tests/LayerCake.ContractTests`.
- C# style rules for each twin (they differ on purpose).
- The locked slice slate, build order, and fallbacks.
- Version freeze rules in `Directory.Packages.props`.
